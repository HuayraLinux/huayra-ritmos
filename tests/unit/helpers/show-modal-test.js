import { showModal } from 'huayra-ritmos/helpers/show-modal';
import { module, test } from 'qunit';

module('Unit | Helper | show modal');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = showModal([42]);
  assert.ok(result);
});
