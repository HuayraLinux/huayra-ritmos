import naturalSort from '../../../utils/natural-sort';
import { module, test } from 'qunit';

module('Unit | Utility | natural sort');

// Replace this with your real tests.
test('it works', function(assert) {
  var result = naturalSort();
  assert.ok(result);
});
