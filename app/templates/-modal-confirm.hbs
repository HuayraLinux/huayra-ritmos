{{#modal-dialog
    close="closeConfirmModal"
    targetAttachment="center"
    translucentOverlay=true}}

<h1 data-test="modal-title">¡Epa!, no guardaste aún</h1>

<p>Parece que hay cambios sin guardar en el proyecto...</p>

<p><strong>¿Que te gustaría hacer?</strong></p>

<div class="vaca-warning"></div>

<div class='center padding'>

  {{#if mustCloseWindowOnDismiss}}
    <button class='ui button' {{action "cancelAndDontCloseFromConfirmModal"}}> No cerrar </button>
    <button class='ui button primary' {{action "saveAndCloseFromConfirmModal"}}> Guardar y salir </button>
    <button class='ui button red' {{action "cancelAndCloseFromConfirmModal"}}> Descartar los cambios, y cerrar </button>
  {{else}}
    <button class='ui button' {{action "cancelAndDontCloseFromConfirmModal"}}> No cerrar </button>
    <button class='ui button primary' {{action "saveFromConfirmModal"}}> Guardar ahora </button>
    <button class='ui button red' {{action "cancelFromConfirmModal"}}> Descartar los cambios </button>
  {{/if}}

</div>

{{/modal-dialog}}
