{{#each steps as |stepslimit|}}
  <input {{action "stepslimitChange" stepslimit.steps}} class="ui button mini {{if stepslimit.seleccionado 'active'}}" type="button" value="{{stepslimit.steps}}" />
{{/each}}
