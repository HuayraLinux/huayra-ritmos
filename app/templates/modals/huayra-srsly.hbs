{{#modal-dialog
    close=close
    targetAttachment="center"
    translucentOverlay=true}}
    <h1>¿Estás seguro de {{accion}}?</h1>
    <button {{action 'ok'}}>{{si}}</button>
    <button {{action 'cancel'}}>{{no}}</button>
{{/modal-dialog}}